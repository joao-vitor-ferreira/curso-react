"use client"

import { ContextUsuario } from "@/components/AulasIntermediario";

const Page = () => {
  return (
    <div className="w-full=screen h-full-screen">
      <ContextUsuario />
    </div>
  );
}

export default Page;